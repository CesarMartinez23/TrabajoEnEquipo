@model IEnumerable<TrabajoEnEquipo.Models.Edificio>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.nombre_edificio)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.direccion_edificio)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.cantidad_oficinas)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.nombre_edificio)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.direccion_edificio)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.cantidad_oficinas)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.idedificio">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.idedificio">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.idedificio">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
